import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#009043',
    padding: 16,
    borderRadius: 32,
    minWidth: '100%',
  },
  itemContainer: {
    alignItems: 'center',
  },
  text: {
    fontWeight: '500',
    fontSize: 14,
    color: 'white',
  },
});

export default styles;
